# Inspect Repo for PlugLayer Deploy

Goals:
- identify deployable units
- choose Dockerfile vs docker-compose
- decide whether the current repo should be built locally and deployed as an image
- infer ports, env vars, and startup commands
