{
  "pluglayer": {
    "command": "/bin/bash",
    "type": "stdio",
    "args": [
      "-lc",
      "if [ -f \"$HOME/.pluglayer/credentials.env\" ]; then . \"$HOME/.pluglayer/credentials.env\"; fi; exec uvx pluglayer-mcp"
    ]
  }
}
